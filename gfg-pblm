class Solution {
    int missingNum(int arr[]) {
        int size = arr.length;
        int xor = 0;

        for (int i = 1; i <= size + 1; i++) {
            xor ^= i;
        }

        for (int i = 0; i < size; i++) {
            xor ^= arr[i];
        }

        return xor;
    }
}
